.vscode/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.claude/
.pytest_cache/
/model
.coverage
/models/
/charts/
/dashboards/
.env
/output/
/themes/
/data/
.DS_Store

# Private companion repo (shelveshq/shelves-internal), cloned + symlinked in.
# See scripts/bootstrap-private.sh. Paths below are symlinks into .private/ —
# no trailing slash, so git ignores the symlink itself (a file, not a dir).
/.private/
/PLAN.md
assets/Shelves Design System.zip
docs/design-system
docs/foundational
docs/plans
